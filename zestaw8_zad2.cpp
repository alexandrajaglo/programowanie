#include <bits/stdc++.h>


int area(int a)
{
    a *= a;
    int area_ =a;
    return area;
}
int area(int a, int b)
{
    a = a*
    int area_ =a;
    return area;
}
double area(double r),
{
    r *=r*3.14
    int area_ =a;
    return area;
}
 using namespace std;

int main()
{
    it a,b;
    cin>>a>>b>>r;
    area(a);
    area(a,b);
    are(r);
    cout<<area(a)<<endl;
    cout<<area(a,b)<<endl;
    cout<<area(r)<<endl;

    return 0;
}
